# Visual regression assets

The `dashboard.png` snapshot is captured from the running FastAPI server (`uvicorn src.server.runtime:app --reload`) and serves as a quick regression check for the canvas rendering, controls, and charts layout.
